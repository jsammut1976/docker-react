language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t jsammut/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jsammut/docker-react npm run test
